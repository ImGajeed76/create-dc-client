const {WebhookClient} = require("discord.js")
const Event = require('../structures/Event');
const {ChalkAdvanced} = require("chalk-advanced");

module.exports = new Event("messageCreate", (client, guild) => {

});
